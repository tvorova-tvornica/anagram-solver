import { Box, Input } from "@chakra-ui/react";
import { FC } from "react";

export const AnagramSolver: FC<{}> = () => {
    return (
        <Box minW="600px">
            <Input placeholder="..." />
        </Box>
    );
};
